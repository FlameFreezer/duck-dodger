const DEBUG = true;

const T_PATTERN_MOVE_SPEED = 400;
const RING_PATTERN_MOVE_SPEED = 200;
const WALL_PATTERN_MOVE_PERIOD = 2000;

const DuckTypes = Object.freeze({
    RUBBER_DUCKY: 0,
    MALLARD: 1,
    GOOSE: 2
});
