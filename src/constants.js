const DEBUG = true;

const T_PATTERN_MOVE_SPEED = 400; // px/sec

const RING_PATTERN_MOVE_SPEED = 200; // px/sec
const RING_PATTERN_GROWTH_RATE = 100; // radius growth rate in px/sec

const WALL_PATTERN_MOVE_PERIOD = 2000; // ms

const Colors = Object.freeze({
    YELLOW: 0,
    GREEN: 1,
    GRAY: 2
});

const DuckTypes = Object.freeze({
    RUBBER_DUCKY: 0,
    MALLARD: 1,
    GOOSE: 2
});
